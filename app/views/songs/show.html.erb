<h1><%= link_to @song.title, song_path(@song) %></h1>
<% if @song.artist %>
<h2><%= link_to @song.artist_name, artist_path(@song.artist) %></h2>
<% else %>
<p><%= link_to "Add Artist", edit_song_path(@song)%></p>
<% end %>

<%= link_to "All Songs", songs_path %>
